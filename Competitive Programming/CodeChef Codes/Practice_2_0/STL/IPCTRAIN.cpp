#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
#define lli long long int
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    lli tn;
    cin >> tn;

    while (tn--)
    {
        int n, d;
        cin >> n >> d;

        for (int i = 0; i < n; i++)
        {
            int day, cls, sad;
            cin >> day >> cls >> sad;
        }
    }
}