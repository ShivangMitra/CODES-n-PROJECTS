#include <iostream>
#include <vector>
using namespace std;

typedef long long int lli;

int main()
{
    lli n, q;

    cin >> n >> q;

    vector<lli> arr(n);

    for (int i = 0; i < n; i++)
    {
        cin >> arr[i];
    }
}