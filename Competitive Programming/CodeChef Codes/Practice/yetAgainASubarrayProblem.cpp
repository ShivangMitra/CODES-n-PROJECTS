#include <iostream>
#include <vector>
using namespace std;

typedef long long int lli;

int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);

    int t;
    cin >> t;

    while (t--)
    {
        lli n, k;

        cin >> n >> k;
        vector<lli> arr(n);
        for (int i = 0; i < n; i++)
        {
            cin >> arr[i];
        }
    }
}